[project]
name = "morsemate-agent"
version = "0.1.0"
description = "MorseMate — LiveKit voice AI Morse-code tutor (backend agent)"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents>=1.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
